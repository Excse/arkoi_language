# This is the entry point for this program
fun main() @u64
    return test((true * ok()) + true, 10.5) * 2.01 - '*'

fun ok() @bool
    return 1.0

fun test(foo @s32, bar @f64) @f32
    return bar * foo